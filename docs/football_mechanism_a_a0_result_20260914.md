# 机制 A：A0 共享梯度探针结果（2026-09-14）

## 结论

A0 路径成立：事件损失与球热图损失在 4/4 个测量点都对同一组 16 个 DINO LoRA 张量产生非零梯度。中位梯度余弦为 `-0.0373`，没有触发预声明的 `-0.20` 冲突降权门槛。

原始 object/event 梯度范数比为 `0.4222, 0.1818, 0.4419, 4.8651`，中位数 `r=0.4321`。按预声明公式 `clip(0.20/r, 0.02, 1.0)` 得到 `0.4629`，四舍五入到实验网格后冻结 `λ*=0.45`；其中位加权梯度比为 `0.1944`，处于目标区间。

step 15 因事件梯度范数仅 `0.2145`，在 `λ=0.45` 下预计出现约 `2.19` 的瞬时辅助/事件梯度比。正式训练必须监控该长尾现象，但不据此修改已预声明的中位数选权规则，也不引入动态权重或 PCGrad。

## 可复现输入与输出

- 原始结果：`outputs/football_events/mechanism_a_20260914/a0_gradient_probe_seed42/mechanism_a_gradient_probe.json`
- 汇总结果：`outputs/football_events/mechanism_a_20260914/a0_gradient_probe_seed42/summary.json`
- 固定辅助损失权重：`0.45`
- 下一步：用完全相同的初始化、数据顺序、优化器预算与评测协议运行 A1（权重 0）和 A2（权重 0.45）。
